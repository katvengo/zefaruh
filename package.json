{
  "name": "zefaruh",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "preBuild": "cd client && npm install",
    "build": "npm run preBuild && cd client && npm run start",
    "start:dev:": "cd client && npm run start"

  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "expo-cli": "^6.3.10"
  }
}
